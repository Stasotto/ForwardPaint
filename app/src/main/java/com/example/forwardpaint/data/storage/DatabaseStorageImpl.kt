package com.example.forwardpaint.data.storage

import com.example.forwardpaint.data.models.UserEntity

class DatabaseStorageImpl : DatabaseStorage {

    override suspend fun signIn(user: UserEntity): Boolean {
        TODO("Not yet implemented")
    }

    override suspend fun signUp(user: UserEntity): Boolean {
        TODO("Not yet implemented")
    }
}